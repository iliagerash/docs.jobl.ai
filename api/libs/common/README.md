# Common Library (`libs/common`)

Shared Python utilities for multiple backend services (`services/api`, `services/sync`, `services/normalize`, `services/training`, `services/inference`).

Planned shared modules:
- configuration helpers
- database/session helpers
- shared schemas and domain models
